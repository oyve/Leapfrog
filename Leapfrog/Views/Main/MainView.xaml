<Window x:Class="Leapfrog.Views.Main.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Leapfrog.Views.Main"
        mc:Ignorable="d"
        Title="Leapfrog" Height="450" Width="800">
    <Window.InputBindings>
        <KeyBinding Command="{Binding Path=RefreshCommand}" Key="F5" />
        <KeyBinding Command="{Binding Path=SelectNavigationCommand}" Key="F6" />
    </Window.InputBindings>
    <DockPanel x:Name="MainDockPanel">
        <ContentControl x:Name="TopArea" DockPanel.Dock="Top" />
        <ContentControl x:Name="MainArea" DockPanel.Dock="Bottom" />
    </DockPanel>
</Window>
